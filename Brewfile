# Curly v0.2 Dependencies
# Install required tools for API testing and data extraction

# Core HTTP client (usually pre-installed on macOS)
brew "curl"

# JSON processor for API responses and configuration
brew "jq"

# Text editor for configuration and response inspection
brew "vim"

# Alternative JSON processor (optional, can replace jq)
# brew "json_pp"
