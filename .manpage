RubyPost V0.1
> An elegant Curl wrapper

COMMANDS:
# HELP
  rp_help       : displays this menu

# SETTINGS
  set_host [host]     : stores the host for easy repeat calls
  set_path [path]     : stores a path for easy repeat calls
  rp_history          : views save request name log
  payload             : quick view contents of payload json
  set_payload         : opens payload json in edit mode
  headers             : quick view contents of headers list
  set_headers         : opens headers list in edit mode

# REQUEST HANDLING
  show_request (request) : displays current request details
  save_request [name]    : saves request
  load_request [name]    : loads specified request
  rp_inspect             : opens last request and response in edit mode
  clear_request          : clears request param files and URL

# HTTP OPERATIONS
  get (url)           : makes a get call with specified headers to the url or saved $URL
  post (url)          : posts specified payload with headers to the url or saved $URL
  open_stream (URL)   : makes a get call to SSE endpoints

# RESET
  reset_rubypost      : wipes the slate clean (including history)
